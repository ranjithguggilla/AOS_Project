# Reference materials (copies)

These files are **copies** of the course/team documents that live in the repository parent folder:

`/Users/guggillaranjith/Desktop/Spring 2026/AOS/Project/`

Original filenames are unchanged in that location. This folder exists so the **maker-platform** tree carries the same references for grading and alignment reviews.

| File | Purpose |
|------|---------|
| `Architecture-AOS.png` | High-level diagram (React+Vite → gateway → services → DB + Redis). |
| `BitForge Presentation.pptx` | BitForge pitch: stack, phases, evaluation, team roles. |
| `BitForge Slides.docx` | Narrative version of BitForge slides. |
| `PPT (midterm check).pptx` | MERN progress snapshot and contributions. |
| `Project Status Update.pptx` | Status, next steps, forum/content roadmap. |
| `Aos_proposal.pptx` | Separate **online learning / LMS** microservices proposal (MongoDB, Node gateway wording). |
| `_extracted_reference_text.txt` | Machine-extracted plain text (for search/diff only; not hand-edited). |
| `REFERENCE_RECONCILIATION.md` | How the **current** `maker-platform` build maps to these documents. |
| `BITFORGE_BUILD_BLUEPRINT_PROMPT.md` | Your consolidated “build blueprint prompt” (kept in-project as execution spec). |
| `BLUEPRINT_ALIGNMENT_STATUS.md` | Requirement-by-requirement status for this repo vs the blueprint prompt. |
